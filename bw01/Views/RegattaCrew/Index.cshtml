@model DataRepository.Models.RegattaCrewViewModel

@{
    ViewBag.Title = "Regatta Crew";
}

<h2>RegattaCrew</h2>

@using (Html.BeginForm("SubmitSelected","RegattaCrew",FormMethod.Post, new { encType = "multipart/form-data", name = "myform" }))
{
    <!-- Start Hidden form -->
    @Html.HiddenFor(m => m.EventCommand)
    @Html.HiddenFor(m => m.EventArgument)
    @Html.HiddenFor(m => m.Mode)
    <!-- End Hidden form-->



   


        <div class="table-responsive">
            <table class="table table-condensed table-bordered table-striped table-hover">
                <thead>
                    <tr>
                        <th>Select</th>
                        <th>Name</th>
                        <th>hidden</th>
                   </tr>
                </thead>
                <tbody>
                    @Html.EditorFor(model=>model.AllCrew)
                </tbody>
            </table>
        </div>
        <input type="submit" name="operation" id="submit" value="Submit Selected" />
    <!-- End Detail Area-->

}

@section scripts
{


}